language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-audioanalyser' ]; then cd .. && eval "mv $currentfolder generator-audioanalyser" && cd generator-audioanalyser; fi

